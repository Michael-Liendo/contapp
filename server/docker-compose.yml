services:
  contapp-database:
    image: 'postgres:16-alpine'
    ports:
      - '5432:5432'
    volumes:
      - contapp-database:/var/lib/postgresql/data
    env_file:
      - .env

volumes:
  contapp-database:
